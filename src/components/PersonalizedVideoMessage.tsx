import React from 'react';

/**
 * Composant temporaire vide - sera supprimé après nettoyage du cache
 * Ce composant a été supprimé mais reste temporairement pour éviter les erreurs de cache
 */
export function PersonalizedVideoMessage() {
  return null;
}
